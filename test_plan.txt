I will update the `title` attribute for the "Copy API Key" button in `apps/console/src/app/(console)/apps/[id]/page.tsx`.

When `apiKey` is empty (before the user rotates the key), copying is disabled, but there's no tooltip explanation. I will change it to provide a helpful message.

Current:
`title="Copy API Key"`

New:
`title={!apiKey ? 'Rotate key to generate a visible API key to copy' : 'Copy API Key'}`

This is a small UX/accessibility win that provides immediate context for a disabled state, matching the memory requirements.
