const fs = require('fs');

// apps/gateway/src/__tests__/gatewayRoutes.integration.test.ts
// imports gateway.test.ts which causes it to fail, checking why it fails in earlier trace:
// "apps/gateway/src/__tests__/gateway.test.ts"
